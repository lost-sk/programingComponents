import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return (
        <div>Please replace Node in index.js</div>
    );
}

ReactDOM.render(<App />, document.getElementById('app'));
